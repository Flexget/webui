{
  "name": "flexget-webui-actions",
  "version": "1.0.0",
  "scripts": {
    "package": "yarn package:skip && yarn package:deploy",
    "package:skip": "ncc build skip-deployment/index.js -o skip-deployment/lib",
    "package:deploy": "ncc build deployment/index.js -o deployment/lib"
  },
  "dependencies": {
    "@actions/core": "^1.2.0",
    "@actions/github": "^1.1.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5"
  }
}
