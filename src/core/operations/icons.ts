import { Sync, PowerSettingsNew, Storage } from '@material-ui/icons';

const icons = {
  reload: {
    Icon: Sync,
    name: 'Reload',
  },
  shutdown: {
    Icon: PowerSettingsNew,
    name: 'Shutdown',
  },
  database: {
    Icon: Storage,
    name: 'Database',
  },
};

export default icons;
